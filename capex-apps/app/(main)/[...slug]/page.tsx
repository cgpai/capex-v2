/**
 * Catch-all route placeholder (e.g. /po-update, /gr-update, /fs-update).
 * Main UI is rendered from `(main)/layout.tsx` via `<App />`.
 */
export default function MainCatchAllPage() {
  return null;
}
