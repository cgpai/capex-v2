/**
 * Legacy flag — app reads/writes Supabase via services; mock auto-seed is disabled separately.
 */
export const USE_MOCK = false;

/**
 * Base URL for the real API.
 */
export const API_BASE_URL = 'http://localhost:3000/api';
